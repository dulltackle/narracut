# #122：仅同步表格内容与持久一步撤回

本票在已有画面设计上交付明确发起的内容同步、完整检查后直接查看、独立输出及跨重开的一步撤回。真实 Agent 首次生成与画面调整仍由 #123 接续；本票验证它们共用的持久发布／撤回边界。

## 验证边界

- `video-update.spec.ts` 主链通过（1.4 分钟），覆盖同步→直接查看→独立输出→取消→编辑→最新无声草稿→关闭重开→撤回，以及通信暂时失败恢复和撤回回执丢失核对。页面主链使用生产工作台、生产工具接口及真实临时项目。准备阶段以公开服务创建存量已接受修订；新同步实际执行检查、不可变 Bundle 构建、执行胶囊代表帧验证及原子发布。
- `video-update.test.ts` 与 `video-update-faults.test.ts` 验证首次生成撤回为空设计、跨重开、第二次成功换代、取消准备请求、最终输入核对期间失权、发布与撤回 rename 前后中断、幂等核对、复制及恢复来源绑定。8 项通过。恢复快照只带原有草稿载荷，发布权威保留在来源核验范围内。
- `portable-project.spec.ts` 实际关闭并移动完整项目，在禁止 registry 网络的环境重建与同步。页面精确帧截图与同 Bundle 的最终 MP4 解码帧逐一对照通过；来源锁文件保持不变。
- `program-preview.spec.ts` 验证单视频精确帧、Bridge 来源与 token 隔离、往返工作区及编辑后遮住旧视频；无设计与失败详情覆盖正常流程的替代入口。
- 存量候选、修订和恢复检查点继续保留。旧页面比较／接受／多版本切换测试被新单设计流程替代，底层接受兼容、完整性与恢复测试继续保留。

## 页面检查

第二轮页面检查覆盖 902×667、960×640、1200×720 与 430×860 CSS px。成功视频位于主面前部，摘要、撤回说明、检查详情和独立输出位于下方；无正常比较／接受入口，窄屏无横向溢出。

截图记录本票真实临时项目完成同步后的页面：

- [902px](synced-902.png)
- [960px](synced-960.png)
- [1200px](synced-1200.png)
- [430px](synced-430.png)

## 真实证据声明

| 范围 | 本次状态 |
| --- | --- |
| 真实图片、视频、MP3 文件，执行胶囊、编码和 Preview = Render | 通过；使用仓库媒体夹具，未用静态页面图片代替视频 |
| 外部 Speech 服务实时合成 | 未执行；使用已有 `hello.mp3`，同步本身不调用 Speech |
| 当前 Codex 宿主真实 Agent 创作 | 未执行；移动项目测试的 Agent 回答为明确标记的可控协议替身 |
| 真实双对话控制权切换 | 未执行；整体宿主与双对话证据沿用 #120 的独立验收范围 |

## 回归说明

完整 `pnpm test` 已执行。Schema、插件构建、打包及 stdio 验证通过；单元阶段出现 4 个超时，涉及复制、恢复、Codex app-server host 与创作任务。对这 4 个文件以单 worker 复测，97 项全部通过。完整页面套件另行执行，首轮 123 项通过、11 项失败；这 11 项在以下修正后的定向复测中均已通过。受影响的 79 项页面回归中 74 项直接通过，其余 4 项历史入口断言及 1 项旧页脚文案断言修正后，定向复测 5 项全部通过。独立核心组的移动项目与单视频协议 3 项通过，主流程最初 5 秒状态等待不足，改为等待保存与轮询核对后的可操作状态，最终主流程通过。

一次只读 OCR 审查发现的准备阶段取消、失败详情显示旧证据、通信恢复后同实例仍遮住三项问题均已修正，并纳入回归。
