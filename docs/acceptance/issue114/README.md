# #114 接受后的独立视频输出

日期：2026-09-13。代码基线为 `06f65f9` 后的本次工作树。

## 行为

视频主面下方保留独立的“输出视频”入口，明确显示“已接受，尚未输出”。点击原位展开来源、时长、画幅、帧率和输出文件夹选择，再由用户点击“开始输出”；接受不启动 Render。收起返回入口焦点，状态刷新不抢焦点。

缺 Speech、缺文件与证据过期直接显示；每项 Scene／文件问题提供定位入口，内容与证据问题并存时仍可进入候选验收。缺 Speech 的 Draft 可以接受，但生成 Speech 后必须重新验收，才能输出。未保存、断连和只读禁止启动。

进度沿用服务真实阶段与已完成帧数。未知回执保留请求身份并查询；取消、执行失败和重试不改变接受事实。成功区域来自服务完成状态，提供实际文件路径、来源修订、文件定位和独立复制路径操作。系统目录选择与文件定位由宿主提供。

## 验证边界

`tests/e2e/final-render.spec.ts` 使用生产页面和协议夹具验证独立展开、取消／核对／重试、选择保持、收起焦点、逐项修复、长路径和窄屏操作。四张 `blocked-*.png` 是 902×667、960×640、1200×720、430×800 CSS px 的阻断状态截图；多项原因可在工作区纵向滚动查看。截图不代表真实媒体或真实 Codex 宿主。

`tests/project-render.test.ts` 使用真实匹配 Speech 文件、PNG Asset、认证执行胶囊与实际 MP4，核验帧数、画幅、帧率、音轨和文件字节。Preview 与 MP4 对应帧复用 `compareVisualFrames`，沿用通道差 30、差异像素比例 0.012；不引入新阈值。覆盖 Draft 接受、补 Speech 后旧证据失效、输入缺项、媒体缺失、重建指纹不符、发布失败与重试、运行期媒体变化、取消及新发现内容失败。

真实 Codex 右侧宿主的系统目录选择、文件定位和用户创作数据输出仍不属于本页协议夹具证据。

## 执行记录

类型检查与插件构建通过。完整套件首次运行中，Schema、独立安装、stdio 检查通过；单元测试 333 通过、2 跳过、5 失败。本次新增 Draft 接受和同 Bundle 帧比较均通过。失败位于宿主超时、创作任务检查、项目复制、项目恢复及双对话控制权，串行复跑这五个文件后 108 项全部通过。

一个只读子代理完成 OCR 审查并复查。发现并修复了不完整输出回执提前解除核对状态，以及只读时禁用复制／定位／收起的问题；同时将文件问题定位到对应 Asset 检查。输出区已完成两轮有界截图核对，修正旧灰色继承造成的对比度和按钮间距。页面回归验证失败原因不会被定时刷新清除。

完整浏览器套件共 123 项，四项因输出入口／接受状态文案变化失败，最后的可移动项目旧进程因等待已改名按钮而结束；更新断言后四项全部通过。最新独立输出五项页面回归全部通过。另以公开配置单独执行真实可移动项目用例通过（2.6 分钟），验证移动后断网重建、接受、实际 MP4 与同 Bundle 帧比较。未把旧进程结束记录成完整套件一次全绿。
