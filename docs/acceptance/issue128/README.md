# #128 独立画面撤回与恢复后同步

基线：`7447480` 加本票实现；日期：2026-09-14（America/Los_Angeles）。生产实现沿用 #122 浅色工作台及执行胶囊，真实 Agent 创作入口由 #123 验证。

## 已执行的行为验证

- `tests/e2e/video-update.spec.ts`：两条真实临时项目流程已通过。通过生产发布事务构造首次生成／调整，页面执行多次同步、独立 MP4 输出、取消、编辑 Narration、关闭、移动和重开。撤回首次生成恢复无设计；撤回调整恢复原程序指纹并同步最新内容，最新 Narration 缺 Speech 时显示草稿警告。全过程保留最新 DSL 和内部候选。
- 同一页面流程覆盖原位确认默认聚焦取消、取消返回入口、Enter 操作和撤回回执丢失后的核对。恢复调整后在同步构建期间外部修改 Brief，生产检查拒绝发布旧输入；页面持续遮住旧视频，说明已恢复但同步失败。取消一次重试后再同步成功，撤回保持已消费。
- `tests/video-update.test.ts` 与 `tests/video-update-faults.test.ts`：11 项通过。覆盖 22 次同步超出普通历史窗口后仍保留正确创作及恢复目标、正常移动、正式复制、原子恢复与增量快照边界、存量 sync 不冒充创作、调整失败／取消保留原记录、成功替换、无连续回退、撤回及同步独立回执、发布／撤回 rename 前后中断、失权和关闭移交竞态。
- 只读 OCR 审查及修复复核完成：关闭会等待恢复提交后的读取，关闭之后不会启动旧项目同步；Brief 独立撤销保持原文案。

## 页面证据

视口：902×667、960×640、1200×720、430×860 CSS px。真实媒体使用仓库内固定 Speech MP3、PNG 与 WebM，通过生产执行胶囊生成 Preview 和独立 MP4；不是实时调用 Speech 服务。

同步后首屏为视频主面，最近画面创作位于同一滚动面的视频下方。确认区截图滚动到原位确认，验证操作至少 44px、无页面横向溢出；异步完成不抢焦点。

| 视口 | 调整撤回确认 | 首次生成撤回确认 |
| --- | --- | --- |
| 902×667 | [截图](confirm-adjust-902.png) | [截图](confirm-generate-902.png) |
| 960×640 | [截图](confirm-adjust-960.png) | [截图](confirm-generate-960.png) |
| 1200×720 | [截图](confirm-adjust-1200.png) | [截图](confirm-generate-1200.png) |
| 430×860 | [截图](confirm-adjust-430.png) | [截图](confirm-generate-430.png) |

[恢复成功但同步失败](restored-sync-failed.png)。四档确认区已逐张检查，默认取消焦点清楚，正文自然换行，确认与取消按钮可达。

## 验收边界

| 项目 | 状态 | 范围 |
| --- | --- | --- |
| 真实媒体与独立 MP4 | 通过 | 固定资产／Speech 文件，生产构建、采帧、发布与输出 |
| 实时 Speech 合成 | 未执行 | 本票同步不调用合成服务 |
| 当前 Codex 宿主自主创作 | 未执行 | #123 接入，整体宿主验收归 #120 |
| 手动发送创作指令 | 未执行 | 本票使用公开发布事务构造完成创作 |
| 自动唤醒 | 未执行 | 恢复后的自动内容同步不等于唤醒 Codex |
| 双对话真实宿主 | 未执行 | 本票覆盖公开事务失权与关闭边界 |

页面夹具提供 App Tool Bridge，后端为生产工具接口与真实临时项目。构造生成／调整时的空验收记录只作为初始已完成创作夹具，不能证明 Agent 创作或对应画面的验收；后续同步、发布、Preview 及输出均执行真实检查，不以夹具记录绕过播放／输出门禁。

## 完整回归记录

`pnpm test` 的 Schema 与插件打包／独立安装／MCP 检查通过；全量 Vitest 首次为 346 通过、3 失败、2 跳过。失败分别是已有宿主适配器的 2 秒文件等待、已有恢复测试的 30 秒超时，以及新增 22 次同步测试的默认 5 秒超时。新增长流程已明确配置 30 秒预算；未改变两个已有测试的断言或超时。

随后以单 worker 复跑三个失败文件及撤回提交点故障文件：4 文件、28 项全部通过。完整页面套件首次为 124 通过、11 失败。10 项失败来自按钮／空状态旧文案和 Brief 提示重复定位，已修正选择器，相关 11 项复跑全部通过。另 1 项暴露预览重建阻塞撤回结果核对，已分离状态轮询和预览加载，并为查询失败后的迟到预览增加失效保护。

最终使用最新插件资源重跑 `program-preview.spec.ts` 与 `video-update.spec.ts`：两条核心真实项目流程、精确帧及无设计状态通过；新增迟到响应测试首次等待两轮轮询超过默认 5 秒，按实际 4 秒轮询周期设置 12 秒等待后单项通过。该测试用页面观察记录确认查询失败期间从未重新显示视频，恢复查询后才能加载。关闭／移动使用页面“关闭项目”并等待退出，避免测试本身在旧页面仍轮询时直接移动目录。

所有本轮发现的失败均已修复或以适当预算复测通过；原全量运行的失败记录保留上述说明，不将其宣称为首次全绿。最终类型检查和 `git diff --check` 通过。
