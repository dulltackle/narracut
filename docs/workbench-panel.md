# 当前 Codex 对话的工作台面板

## 能力依据与入口

2026-09-10 的当前会话实际提供 `mcp__codex_app__open_in_codex`：`placement: "right"` 配合 `target: {type: "browser", url}`，省略 `threadId` 时由宿主定位调用对话。插件通过 `skills/narracut-workbench/SKILL.md` 使用该宿主工具，启动 `panel.mjs` 并打开其本地 URL。没有该工具时，在当前对话显示能力不足，等待重试。

当前对话 Shell 的 `CODEX_THREAD_ID` 为 `01a08ab9-8932-7f43-a960-eabfc9646c5c`；本轮用宿主 `read_thread` 核对了相同 ID、工作目录及 #93 请求。启动技能要求每次从当前对话核对来源，再把环境身份绑定到该面板进程。浏览器请求参数不能改变绑定；共享 stdio MCP 进程不借用其启动环境猜测当前调用者。

[OpenAI 插件概览](https://learn.chatgpt.com/docs/plugins)说明插件可以组合技能、MCP 与可选 UI；[UI 参考](https://developers.openai.com/plugins/reference)描述的是 UI 运行时接口，本轮未从中找到能保证 Codex 当前对话右侧位置与对话 ID 的契约。因此此入口使用会话中实际提供的宿主工具，不新增私有调用，也不把 MCP display mode、返回 HTML 或 UI 握手解释成桌面位置证据。

## 运行边界

- `panel.mjs` 监听随机回环端口，以随机秘密路径承载外壳、原有工作台资源和工具转发。校验 Host、Origin、JSON 内容类型，关闭缓存与 referrer，秘密 URL 不进入文档或 issue。
- 外壳复用原工作台的 MCP Apps 握手与工具操作；项目创建、打开、Scene 保存、Asset、Preview、接受与最终 Render 仍走同一项目服务与门禁。
- 面板向既有 UI 注入自有文件选择适配器，将 `selectDirectory`、`selectFile`、`selectFiles` 转发到本地系统选择工具；覆盖启动器、Asset 导入、Render 选址及恢复/LOCAL 导出。原生命令由固定种类生成，未执行用户提供的标题或 Shell。
- `get_workbench` 读取本次会话最新项目与任务状态。关闭页面不会释放项目或终结任务，重新打开相同 URL 不重新创建项目。用户结束进程才释放服务资源。
- 创建或打开的持久结果与面板展示独立。`prepared` 表示服务就绪，宿主 `queued` 表示请求排队，两者都不表示已显示。服务失败的原因在当前对话报告；页面加载失败保留“重试显示工作台”，只重新读取当前会话。
- 没有可核实对话时，生产 stdio 入口与面板拒绝项目写工具；提供只读检查与具体原因。仅内部测试调用可不配置对话上下文。既有表格编辑和渲染条件仍由原服务执行。
- 项目名保持可见，路径、项目 ID 与对话 ID 收进详情；面板使用当前对话 Composer，底部仅保留说明。Scene 表格首开，窄面板保留检查抽屉、Asset 管理与 Agent 审阅入口。

跨对话查看与明确接管见下节。共享 stdio 无法确认调用对话时仍走能力不足路径；绑定到当前对话的面板通过相同 MCP 工具桥执行项目操作。

## 本轮验收记录

环境：Linux x64、Node 22.23.2、本地仓库构建产物；未将旧安装缓存当作新实现。宿主工具可调用，原生桌面截图能力不可用（CUA 枚举超时）。

| 检查 | 证据与边界 |
| --- | --- |
| 当前对话身份 | Shell 与 `read_thread` 对应相同 ID、目录、#93 请求 |
| 真实宿主面板打开 | `open_in_codex(right, browser)` 返回 `queued` 与上述当前对话 ID；恢复已停止的服务并用 `--open` 打开原项目后，用户在当前对话明确确认“右侧已经出现工作台”；尚无原生桌面截图 |
| 真实入口临时项目 | `panel.mjs --create` 在 `/tmp/narracut-issue93-desktop-20260910` 创建项目，返回 `valid`；项目 ID 为 `88163d56-2674-458d-abcc-0bd6f8ab77c9` |
| 公开入口自动化 | `tests/workbench-panel.test.ts` 验证创建、刷新、未知身份拦截与其他网站请求拒绝；`tests/e2e/workbench-panel.spec.ts` 用真实 HTTP 与项目服务验证启动器创建/打开、Scene 保存重载、Asset 导入绑定、Preview/Render 入口、错误与重试；系统选择结果由宿主边界模拟，Render 选址用已就绪的服务状态夹具 |
| 响应式视觉 | Chromium 下 1440、680、390 × 900 截图核对接触表、项目身份、底部说明和无横向溢出；这些是测试浏览器截图，不能替代 Codex 桌面截图 |

#93 已获得用户对当前对话右侧显示的明确确认，但三个入口的完整桌面操作、真实目录选择与取消及可追溯桌面截图尚未全部补齐，因此本记录不宣称本票完整通过。

本轮执行完整 `pnpm test`：格式检查 19 条、单元 311 条通过（2 条环境限定用例跳过）、端到端 86 条通过。独立代码审查发现的文件选择器缺口和身份失效重载错误已修复，并补充定向测试；新增面板产物另在仓库外的中文空格路径验证启动、资源及绑定身份。全套中的实际 Preview、接受、同 Bundle 最终 Render 回归不等同于真实 Codex 桌面位置验收。


## fixtures/clips 实际素材补验（2026-09-10）

用户确认右侧工作台出现后，明确指定仓库 `fixtures/clips` 进行测试。本轮复用上述项目与服务，通过同一公开 HTTP 面板在 Chromium 中操作；桌面控制接口仍超时，以下截图是自动化浏览器证据，不是 Codex 桌面截图。系统文件选择返回值由测试提供，导入、编辑、保存、状态读取与门禁使用真实项目服务。未修改源素材。

- 将 `script.md` 的 13 行原文按顺序填入 13 个 Scene，分别导入并绑定 `1.png`、`2.mp4` 至 `13.mp4`。逐项保存通过；刷新后 13 个 Scene、文稿与 Asset 绑定全部保留。
- 13 个导入副本与各自源文件的 SHA-256 全部一致。`clips.zip` 不作为媒体导入。
- 展开工作台关联信息，浏览器中可见的对话 ID 与服务绑定 ID 一致。
- 点击当前修订 Preview 构建，真实返回“当前修订构建失败：依赖声明与根锁图不一致。”因此本次未获得可播放 Preview，不将入口可点击计为预览成功。
- 最终 Render 显示“尚未接受”“Render 已阻断”，准备按钮禁用，并提示前往候选检查与接受流程。13 个 Scene 尚无 Speech，TTS 未配置；本轮未接受候选、未生成 Speech、未生成成片，也未绕过门禁。
- 在测试浏览器的状态读取边界注入一次 HTTP 503，确认显示具体原因，点击“重试显示工作台”后恢复。项目 ID 不变，13 个 Scene 和 13 个 Asset 保留，无新建项目。

临时证据位于 `/tmp/narracut-93-clips-result.json`、`/tmp/narracut-93-clips-integrity.json`、`/tmp/narracut-93-clips-table.png`、`/tmp/narracut-93-clips-gates.png`、`/tmp/narracut-93-clips-retry.png`。初次测试脚本误用双击进入编辑及尝试点击已禁用 Render 按钮，修正为显式编辑按钮和禁用状态断言后完成补验；这两次脚本定位失败不计为产品缺陷。


## 验收限制定位（2026-09-10）

使用 diagnosing-bugs 做最小对照，未修改宿主配置或绕过工具权限：

| 探针 | 结果 | 能证明的结论 |
| --- | --- | --- |
| `await cua.getState()`，15 秒调用预算 | 超时并重置 REPL | 全局枚举不可靠；单凭此结果不能认定内置浏览器不可用 |
| 直接 `cua.createBrowserTab('iab', 'about:blank', {visible:false})` | 约 1.2 秒成功 | 内置浏览器连接可用 |
| `cua.listBrowsers()` | 约 0.02 秒成功，列出 Chrome 扩展及内置浏览器 | 提供方发现可用 |
| `cua.listTabs({browser:'1'})`（本轮内置浏览器 ID） | 约 0.02 秒成功 | 内置标签枚举可用 |
| `cua.listTabs({browser:'2'})`（本轮 Chrome 扩展 ID） | 15 秒超时 | 超时被定位至 Chrome 扩展标签枚举路径；扩展侧为何不响应仍未取得证据 |
| 内置浏览器直接访问当前工作台，读取 AX 与截图 | 成功，显示真实项目与 13 个 Scene | 工作台页面可以通过 CUA 检查；此前只尝试全局枚举，过早将其当成所有读取能力的阻断 |
| `open_in_codex` 改为打开本地 Markdown 文件 | 同样返回 `queued` 与当前 Thread ID | 排队回执与 Narracut HTML、HTTP 服务及文件类型无关；具体宿主排队条件尚未定位 |
| 从内置浏览器点击真实“导入并绑定” | 工作台等待选择；出现由面板进程启动的 Zenity 子进程 | 真实选择桥接已执行；不是未安装 Zenity。窗口是否可见与人工取消结果需用户确认 |

当前工具明确禁用原生应用 API；尝试其通用文档列出的 `cua.listApps` 也返回函数不存在。Linux 文件选择由 `directory-picker.ts` 启动独立 Zenity 窗口，不是浏览器 DOM 或 HTML 文件输入。故浏览器自动化不能完成该系统窗口的点击与截图；不应通过模拟文件路径声称已经验收原生选择窗口。环境中存在 `/usr/bin/zenity`，`DISPLAY=:0`，`XDG_SESSION_TYPE=x11`。

本轮已证实的操作替代路径是直接选择内置浏览器、读取工作台 AX 与页面截图，避免等待无关 Chrome 提供方的全局枚举。这能解除页面级验收阻断，但页面截图仍不包含 Codex 外层对话与右侧面板位置。`queued` 是宿主回执，本地 `codex-app-tools` 包装层转发调用，未实现可供本轮验证的展示完成查询；尚无证据将其归因为焦点错误或 Thread 绑定错误。

## 跨对话只读会话与明确接管（#94）

仅打开已占用项目时，不申请交接、不释放租约、不停止旧任务。只读面板绑定规范化的物理目录与项目 ID，通过现有租约中的回环端点读取持有进程的最新状态。端点使用租约能力令牌鉴权，并逐项允许只读工具；Scene、Asset、Speech、Brief、候选、依赖协调、检查执行、交付确认、接受、Render、恢复及复制写操作默认拒绝。浏览器参数不能改变已核实的对话身份。

当前对话明确请求 project_control/takeover 时，旧进程先封闭新的写请求、使已接收请求的权限代次失效，再撤销创作驱动并等待旧 Turn 的中断回执。请求权限在持久提交边界再次核对；旧 Speech 等后台执行也会取消。只有旧操作收尾、旧租约释放后，新会话才通过独占创建取得同一物理目录的租约。失败保留阻断和具体原因，重复明确接管用于核对重试；并发申请仍由进程租约决定唯一获胜者。

仅接管保留候选及任务 ID，不发起或继续创作、不接受或放弃候选。明确发起或继续创作复用相同屏障，并在有可信身份时绑定当前 Codex 对话。插件技能提供同一面板 RPC 调用路径；没有可复用面板时用 panel.mjs --take-control。

两个工作区共用顶部权限提示，连接状态独立显示。状态轮询让新旧面板更新权限；状态无法确认时暂停编辑。失权后清除自动保存计时器，保留本地 Scene 与 Brief 草稿，提供逐 Scene 旁白对照、最新内容及复制入口。重新获得控制权先读取最新内容，用户手动提交核对后的草稿；旧回执不能覆盖失权后的界面状态。

只读面板保留 Scene、Asset、Brief 和已有审阅结果的浏览入口。Preview 的 view 仅为已就绪 Bundle 和媒体字节复制独立的内存绑定，使用当前面板 origin、独立实例 ID 和令牌；不启动构建，不增加接受证据。build、检查执行和 Render 在只读会话均被拒绝。只读绑定有数量上限，项目清理时释放；失去来源实例后只能显示过期状态。

验收入口：tests/workbench-panel.test.ts 覆盖双对话 MCP 桥、公开 HTTP、全类写请求拒绝、明确继续、失败重试、旧 Turn 与 Speech 迟到结果、独立进程租约和只读 Preview 绑定；tests/project-control-race.test.ts 在身份及基线的最后磁盘校验中触发交接，验证提交前撤权；tests/e2e/workbench-panel.spec.ts 覆盖顶部状态、只读构建禁用、草稿保留、重新获得权限后手动保存和窄屏溢出。以上使用可控宿主替身，不代表真实 Codex 双对话桌面操作的录像证据。

## 当前对话继续已停止任务（#96）

用户明确继续时，服务校验单一任务检查点及当前候选基线，保持任务 ID，绑定发出指令的当前对话，并重新读取项目内容。应用重启仅加载为已停止，不启动创作；旧 Turn 未提交的修改、工具调用及中间判断不属于恢复状态。重启或交接后旧工具审批已失效，继续时重新判断和申请审批，旧回执不能恢复执行。

跨对话继续复用控制权交接屏障。撤销旧驱动并等待持久批次完成后，必须核对磁盘仍是本驱动最后成功写入的检查点、候选仍是已完成批次的基线，才封存交接状态；候选已原子提交但检查点尚在收尾时保留该成果。不能用旧内存补写缺失、损坏或不一致的检查点。无效时保留原持久内容并报告 `TASK_CHECKPOINT_INVALID`；用户仍可明确用新目标、按当前候选基线建立新任务。

验证入口为 `tests/creation-task.test.ts`：覆盖原子候选提交前后停止、重启后明确继续、Codex 中断、最新 Brief、跨对话写权与迟到结果、审批中重启或主动停止、候选提交后暂停收尾的交接，以及交接前注入三类检查点故障。使用公开 MCP 工具与可控宿主替身验证稳定状态及持久内容，不依赖模型随机输出；不作为真实 Codex 桌面交互的验收证据。

## 候选审阅工作区（#97）

Agent 工作区按任务状态、Preview、完整交付、整体决策和最终 Render 纵向排列。当前创作指令、技术身份与详细检查可展开；目标、变更摘要、新鲜度、阻断原因、警告及 Scene 建议保持可访问。工作台不再提供聊天输入、开始或继续创作、新目标接管及 Brief 对话审批入口；这些创作指令由当前 Codex 对话承载。停止任务、接受与放弃仍是明确的工作台操作。

首次读取已有 Preview 时优先候选。“对比当前”与“返回候选”在至多两个槽位间比较，同时只展示和播放一个；切换暂停原播放器。当前画面仍在播放时收到新候选，仅提示“新候选已就绪”。隐藏播放器暂停，普通工作区切换保留版本和 Scene 选择，不触碰 Codex Composer。

Scene 建议提供完整值复制和稳定 ID 定位，用户在表格中手动修改；删除的 Scene 禁用定位并解释原因。接受沿用服务端最新证据门禁；放弃要求确认删除范围。写权不足时禁用写操作，服务端再次验证控制权。提交回执不明时保留核对状态并阻止重复提交。

回归入口：`pnpm test:e2e:public` 包含真实可移动项目的当前对话、交付、比较、复制定位、接受和最终 Render，以及公开双面板的只读拒绝、放弃和工作区切换。`program-preview.spec.ts` 补充失败、过期、新候选到达和播放器连续性；`candidate-delivery.spec.ts` 补充完整代表帧、分页和已删除 Scene。测试使用可控宿主驱动和真实工作台，不声称验证了 Codex 桌面 Composer 的宿主内部状态。

## 关闭与重新同步（#98）

页面卸载只断开显示；面板 HTTP 服务与创作任务由当前对话的服务进程持有，不监听浏览器卸载来关闭项目、停止任务或释放写权。重开应复用原 URL。只有明确结束服务或进程退出才调用服务的 `close()`；服务重新启动后仍按 `APP_RESTARTED` 读取停止检查点，不能将它当成页面重开。

新页面初始化先显示“正在同步最新项目与任务状态…”。`get_workbench` 读取最新项目、候选、任务和控制权，工作台处理结果并完成渲染后才解除页面的 `inert` 与同步遮罩。失败显示具体原因、“任务状态尚未核实”和“重试显示工作台”。超时会中止当前请求；重试销毁旧 iframe，以独立浏览上下文和请求代次隔离迟到响应，不借用旧 Preview 或旧可编辑状态。

运行状态附近说明“关闭面板后，任务仍会继续。需保持应用与 Codex 任务运行。”重开、读取和重试不调用开始、继续、接管、接受或 Render。候选就绪仍由用户审阅；应用重启、Codex 中断与明确停止保留各自原因。

### 自动化复验

执行 `pnpm build:plugin` 后运行 `pnpm exec playwright test tests/e2e/panel-reconnect.spec.ts`。测试通过真实面板 HTTP 和工作台页面，仅在 Codex 宿主边界使用 `ControlledCreationHost`：

- 销毁原页面后提交候选资源，核对候选文件、原子任务检查点与 Task ID，再创建新页面检查运行状态与权限。
- 另一对话打开仍只读；重开没有额外 Turn、停止或接管。
- 对照明确停止、Codex 中断、服务退出重启、无任务和已终结状态。
- 延迟与 503 故障验证同步门禁、失败重试、迟到结果隔离；候选就绪的 UI 分支使用状态响应夹具，真实交付门禁由 `creation-task.test.ts` 和候选交付套件覆盖。

这些自动化不调用真实模型，不消耗账户额度，也不替代真实 Codex 桌面验收。

### 真实桌面关闭／重开复验步骤

1. 从当前 Codex 对话核实身份，按插件工作台技能打开测试项目，记录服务 URL、项目目录、Project ID 和当前 Task ID。
2. 在当前对话明确发起一项会修改候选资源的创作；在 Agent 工作区确认“运行中”，记录候选 baseline 和最后保存阶段。
3. 使用 Codex 右侧面板自身的关闭入口关闭工作台，保持 Codex 应用、当前创作和服务进程运行。不要发送“停止任务”，也不要终止服务。
4. 等待当前对话产生新的持久成果。通过同一服务 `get_workbench` 读取任务和候选，记录 Task ID、候选 baseline，以及候选目录中实际新增或修改的文件内容。
5. 在同一对话用 `open_in_codex` 重新打开原 URL；观察先同步再显示最新内容。核对 Project ID、Task ID、最新候选和控制权；确认没有新任务、自动播放、自动接受或自动 Render。
6. 从另一对话打开同一项目，核对“只读 · 项目由另一对话控制”与禁用写操作；原任务和控制权保持不变。
7. 分别以用户明确停止、Codex 中断、退出并重启应用进行对照。重开应显示实际停止原因和保留成果，只有明确继续才再次运行；记录每种情况的 Task ID 与原因。
8. 记录验收日期、Codex 版本、对话身份、截图路径、候选文件证据和每步结果。真实模型不可用、额度不足或无法操作桌面关闭入口时，将对应步骤标为“未执行”，不得用自动化替代通过。

本票实现期间的真实桌面关闭／重开综合步骤尚未执行；本文提供复验流程，不声明宿主桌面验收通过。
